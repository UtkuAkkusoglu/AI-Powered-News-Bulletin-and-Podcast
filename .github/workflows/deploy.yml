name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main # main branch'ine push geldiğinde otomatik çalışacak

env:
  PROJECT_ID: project-9b6d702e-bc81-4d20-aff       
  GAR_LOCATION: europe-west3           # Frankfurt bölgesi
  REPOSITORY: news-podcast-repo        # Artifact Registry'deki depo adın
  REGION: europe-west3

jobs:
  # ==========================================
  # 1. AŞAMA: BUILD (12-Factor: Build)
  # ==========================================
  build-artifacts:
    name: Build & Push Docker Images
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Google Kimlik Doğrulama
      - name: Google Auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Docker'ı Google Altyapısı İçin Yetkilendirme
      - name: Authorize Docker
        run: |
          gcloud auth configure-docker ${{ env.GAR_LOCATION }}-docker.pkg.dev --quiet

      # Docker Login işlemini Google eklentisiyle garantiye alıyoruz
      - name: Docker Login to GAR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GAR_LOCATION }}-docker.pkg.dev
          username: _json_key
          password: ${{ secrets.GCP_SA_KEY }}

      # API Imajını Build Et ve Artifact Registry'ye Fırlat
      - name: Build & Push API Image
        run: |
          docker build -t ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/api:${{ github.sha }} .
          docker push ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/api:${{ github.sha }}

      # Frontend Imajını Build Et ve Artifact Registry'ye Fırlat (Canlı API URL'i ile birlikte!)
      - name: Build & Push Frontend Image
        run: |
          docker build \
            --build-arg VITE_API_URL=https://news-and-podcast-api-861840374112.europe-west3.run.app \
            -t ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/frontend:${{ github.sha }} ./frontend
          docker push ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/frontend:${{ github.sha }}

  # ==========================================
  # 2. AŞAMA: DEPLOY (12-Factor: Release & Run)
  # ==========================================
  deploy-to-prod:
    name: Deploy to Google Cloud Run
    runs-on: ubuntu-22.04
    needs: build-artifacts
    steps:
      - name: Google Auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # API'yi Canlıya Al (Redis Gizlendi!)
      - name: Deploy Backend to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: news-and-podcast-api
          image: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/api:${{ github.sha }}
          region: ${{ env.REGION }}
          flags: |
            --allow-unauthenticated 
            --port=8080
            --service-account=news-and-podcast-sa@project-9b6d702e-bc81-4d20-aff.iam.gserviceaccount.com
            --set-secrets=DATABASE_URL=DATABASE_URL:latest,SECRET_KEY=SECRET_KEY:latest,POSTGRES_USER=POSTGRES_USER:latest,POSTGRES_PASSWORD=POSTGRES_PASSWORD:latest,CELERY_BROKER_URL=CELERY_BROKER_URL:latest
            --set-env-vars=POSTGRES_DB=news_and_podcast,PGDATA=/var/lib/postgresql/data/pgdata,GCP_PROJECT_ID=project-9b6d702e-bc81-4d20-aff,GCP_BUCKET_NAME=news-and-podcast-storage,FRONTEND_URL=https://news-and-podcast-frontend-861840374112.europe-west3.run.app

      # Frontend'i Canlıya Al
      - name: Deploy Frontend to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: news-and-podcast-frontend
          image: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/frontend:${{ github.sha }}
          region: ${{ env.REGION }}
          flags: '--allow-unauthenticated --port=8080'

      # AMELE WORKER: Scraper İşlerini Yapar
      - name: Deploy Scraper Worker to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: news-and-podcast-scraper-worker
          image: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/api:${{ github.sha }}
          region: ${{ env.REGION }}
          flags: |
            --allow-unauthenticated
            --no-cpu-throttling
            --service-account=news-and-podcast-sa@project-9b6d702e-bc81-4d20-aff.iam.gserviceaccount.com
            --set-secrets=DATABASE_URL=DATABASE_URL:latest,SECRET_KEY=SECRET_KEY:latest,POSTGRES_USER=POSTGRES_USER:latest,POSTGRES_PASSWORD=POSTGRES_PASSWORD:latest,CELERY_BROKER_URL=CELERY_BROKER_URL:latest
            --set-env-vars=POSTGRES_DB=news_and_podcast,PGDATA=/var/lib/postgresql/data/pgdata,GCP_PROJECT_ID=project-9b6d702e-bc81-4d20-aff,GCP_BUCKET_NAME=news-and-podcast-storage
            --command="celery"
            --args="-A worker.celery_app worker --loglevel=info --concurrency=1 -Q scraper_queue"

      # VIP WORKER: AI ve Podcast İşlerini Yapar
      - name: Deploy AI Worker to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: news-and-podcast-ai-worker
          image: ${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/api:${{ github.sha }}
          region: ${{ env.REGION }}
          flags: |
            --allow-unauthenticated
            --no-cpu-throttling
            --service-account=news-and-podcast-sa@project-9b6d702e-bc81-4d20-aff.iam.gserviceaccount.com
            --set-secrets=DATABASE_URL=DATABASE_URL:latest,SECRET_KEY=SECRET_KEY:latest,POSTGRES_USER=POSTGRES_USER:latest,POSTGRES_PASSWORD=POSTGRES_PASSWORD:latest,CELERY_BROKER_URL=CELERY_BROKER_URL:latest
            --set-env-vars=POSTGRES_DB=news_and_podcast,PGDATA=/var/lib/postgresql/data/pgdata,GCP_PROJECT_ID=project-9b6d702e-bc81-4d20-aff,GCP_BUCKET_NAME=news-and-podcast-storage
            --command="celery"
            --args="-A worker.celery_app worker --loglevel=info --concurrency=4 -Q ai_queue"